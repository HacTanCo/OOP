#define INPFILE "TP.INP"
#define OUTFILE "TP.OUT"

#include <bits/stdc++.h>
using namespace std;

int main(void) {
	if (fopen(INPFILE, "r")) {
		freopen(INPFILE, "r", stdin);
		freopen(OUTFILE, "w", stdout);
	}

	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);



	return 0-0;
}

